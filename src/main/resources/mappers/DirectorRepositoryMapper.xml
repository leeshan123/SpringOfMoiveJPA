<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.moviespring.web.repository.DirectorRepository">
    <insert id="save" parameterType="Director">
        insert into menu_like (kobis_id, tmdb_id, kor_name, eng_name, img_url, popularity)
        values (#{menuLike}, #{tmdbId}, #{korName}, #{engName}, #{imgUrl}, #{popularity})
    </insert>

</mapper>